import Wu04BypassMatrix

namespace WuTarget.W08
open Wu2008DoubleSieve SharpLogRecurrence JointLogTotalComparison
open FirstFeedbackIntegrals FirstErrorFullPayment RemainingHf
open F1FullRecoveryPayment Wu04FactorEnvelopes

theorem terminal_first_cell_strict :
    Wu04Bypass.cell 3 1 (11 / 5) < paidCell 3 1 (11 / 5) := by
  norm_num [Wu04Bypass.cell, paidCell, cellScale, beta, signed, splitLower, splitUpper,
    basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe, residualPrimitive,
    residualDenom, leftFactor, rightFactor, lowerLog, upperLog, V, lowerGapPayment,
    upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

end WuTarget.W08
