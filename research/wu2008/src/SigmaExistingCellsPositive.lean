import SigmaExistingProfile
namespace SigmaExistingLogError
open Real NodeExtension FirstFeedbackIntegrals OriginalSigmaStrength
open Wu04WholeCollection Wu2008DoubleSieve Wu04FactorEnvelopes SharpLogRecurrence JointLogTotalComparison
noncomputable section
theorem original_cell_0_pos : 0<cellPaid (upperLeft (0 : Fin 9)) (upperNode (0 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_1_pos : 0<cellPaid (upperLeft (1 : Fin 9)) (upperNode (1 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_2_pos : 0<cellPaid (upperLeft (2 : Fin 9)) (upperNode (2 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_3_pos : 0<cellPaid (upperLeft (3 : Fin 9)) (upperNode (3 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_4_pos : 0<cellPaid (upperLeft (4 : Fin 9)) (upperNode (4 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_5_pos : 0<cellPaid (upperLeft (5 : Fin 9)) (upperNode (5 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_6_pos : 0<cellPaid (upperLeft (6 : Fin 9)) (upperNode (6 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_7_pos : 0<cellPaid (upperLeft (7 : Fin 9)) (upperNode (7 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

theorem original_cell_8_pos : 0<cellPaid (upperLeft (8 : Fin 9)) (upperNode (8 : Fin 9)) := by
  norm_num [cellPaid,poleCellPaid,upperNode,upperLeft,signedLow,
    low,up,lower,upper,leftFactor,rightFactor,lowerLog,upperLog,V]

end
end SigmaExistingLogError
