import SrcFourEnclosureData

noncomputable section
open Wu2008DoubleSieve FourRoughClosedMass
namespace WuSource.SrcFourEnclosure

theorem cell_large_48 :
    largeLower 48 ≤ largeLo (midpoint cut beta 48) ∧
      largeHi (midpoint cut beta 48) ≤ largeUpper 48 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_49 :
    largeLower 49 ≤ largeLo (midpoint cut beta 49) ∧
      largeHi (midpoint cut beta 49) ≤ largeUpper 49 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_50 :
    largeLower 50 ≤ largeLo (midpoint cut beta 50) ∧
      largeHi (midpoint cut beta 50) ≤ largeUpper 50 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_51 :
    largeLower 51 ≤ largeLo (midpoint cut beta 51) ∧
      largeHi (midpoint cut beta 51) ≤ largeUpper 51 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_52 :
    largeLower 52 ≤ largeLo (midpoint cut beta 52) ∧
      largeHi (midpoint cut beta 52) ≤ largeUpper 52 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_53 :
    largeLower 53 ≤ largeLo (midpoint cut beta 53) ∧
      largeHi (midpoint cut beta 53) ≤ largeUpper 53 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_54 :
    largeLower 54 ≤ largeLo (midpoint cut beta 54) ∧
      largeHi (midpoint cut beta 54) ≤ largeUpper 54 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_55 :
    largeLower 55 ≤ largeLo (midpoint cut beta 55) ∧
      largeHi (midpoint cut beta 55) ≤ largeUpper 55 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_56 :
    largeLower 56 ≤ largeLo (midpoint cut beta 56) ∧
      largeHi (midpoint cut beta 56) ≤ largeUpper 56 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_57 :
    largeLower 57 ≤ largeLo (midpoint cut beta 57) ∧
      largeHi (midpoint cut beta 57) ≤ largeUpper 57 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_58 :
    largeLower 58 ≤ largeLo (midpoint cut beta 58) ∧
      largeHi (midpoint cut beta 58) ≤ largeUpper 58 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_59 :
    largeLower 59 ≤ largeLo (midpoint cut beta 59) ∧
      largeHi (midpoint cut beta 59) ≤ largeUpper 59 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_60 :
    largeLower 60 ≤ largeLo (midpoint cut beta 60) ∧
      largeHi (midpoint cut beta 60) ≤ largeUpper 60 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_61 :
    largeLower 61 ≤ largeLo (midpoint cut beta 61) ∧
      largeHi (midpoint cut beta 61) ≤ largeUpper 61 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_62 :
    largeLower 62 ≤ largeLo (midpoint cut beta 62) ∧
      largeHi (midpoint cut beta 62) ≤ largeUpper 62 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_63 :
    largeLower 63 ≤ largeLo (midpoint cut beta 63) ∧
      largeHi (midpoint cut beta 63) ≤ largeUpper 63 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

end WuSource.SrcFourEnclosure
