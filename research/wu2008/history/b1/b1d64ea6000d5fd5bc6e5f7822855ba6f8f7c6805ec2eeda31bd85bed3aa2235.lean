import W02Rational

namespace WuTarget.W02

theorem output0 : (11285954 : ℚ) / 1000000000 ≤ qOutput 0 := by
  norm_num [qOutput, qMatrix, qPaid, qTail, qNode, qUpper, qLeft, qLogLower,
    qV8, gridIndices, NodeExtension.gridStart, Fin.sum_univ_succ,
    Finset.sum_Icc_succ_top]

end WuTarget.W02
