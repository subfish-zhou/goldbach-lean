import W08KernelMatrix

namespace WuTarget.W08
open Wu2008DoubleSieve SharpLogRecurrence JointLogTotalComparison
open ActualNineFeedback NodeExtension FirstFeedbackIntegrals FirstErrorFullPayment
open RemainingHf F1FullRecoveryPayment Wu04FactorEnvelopes

theorem paidMatrix_row0 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 0 k < paidMatrix 0 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, coupledRow, SecondFunctionalPositive.parameters,
      SecondFunctionalParameters.row1, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row1 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 1 k < paidMatrix 1 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, coupledRow, SecondFunctionalPositive.parameters,
      SecondFunctionalParameters.row2, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row2 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 2 k < paidMatrix 2 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, coupledRow, SecondFunctionalPositive.parameters,
      SecondFunctionalParameters.row3, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row3 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 3 k < paidMatrix 3 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, coupledRow, SecondFunctionalPositive.parameters,
      SecondFunctionalParameters.row4, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row4 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 4 k < paidMatrix 4 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, firstS, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row5 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 5 k < paidMatrix 5 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, firstS, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row6 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 6 k < paidMatrix 6 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, firstS, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row7 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 7 k < paidMatrix 7 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, firstS, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

theorem paidMatrix_row8 (k : Fin 9) :
    Wu04Bypass.elementaryMatrix 8 k < paidMatrix 8 k := by
  fin_cases k <;>
    norm_num [Wu04Bypass.elementaryMatrix, paidMatrix, Wu04Bypass.cell, cellLeft,
      upperNode, upperLeft, firstS, paidCell, cellScale, beta, signed,
      splitLower, splitUpper, basicLower, basicUpper, ea, eb, ec, ed, qa, qb, qc, qd, qe,
      residualPrimitive, residualDenom, leftFactor, rightFactor, lowerLog, upperLog,
      V, lowerGapPayment, upperGapPayment, F1LowerResidual.payment, F1LowerResidual.denom]

end WuTarget.W08
