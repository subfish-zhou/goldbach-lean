import SrcFourEnclosureData

noncomputable section
open Wu2008DoubleSieve FourRoughClosedMass
namespace WuSource.SrcFourEnclosure

theorem cell_large_16 :
    largeLower 16 ≤ largeLo (midpoint cut beta 16) ∧
      largeHi (midpoint cut beta 16) ≤ largeUpper 16 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_17 :
    largeLower 17 ≤ largeLo (midpoint cut beta 17) ∧
      largeHi (midpoint cut beta 17) ≤ largeUpper 17 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_18 :
    largeLower 18 ≤ largeLo (midpoint cut beta 18) ∧
      largeHi (midpoint cut beta 18) ≤ largeUpper 18 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_19 :
    largeLower 19 ≤ largeLo (midpoint cut beta 19) ∧
      largeHi (midpoint cut beta 19) ≤ largeUpper 19 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_20 :
    largeLower 20 ≤ largeLo (midpoint cut beta 20) ∧
      largeHi (midpoint cut beta 20) ≤ largeUpper 20 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_21 :
    largeLower 21 ≤ largeLo (midpoint cut beta 21) ∧
      largeHi (midpoint cut beta 21) ≤ largeUpper 21 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_22 :
    largeLower 22 ≤ largeLo (midpoint cut beta 22) ∧
      largeHi (midpoint cut beta 22) ≤ largeUpper 22 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_23 :
    largeLower 23 ≤ largeLo (midpoint cut beta 23) ∧
      largeHi (midpoint cut beta 23) ≤ largeUpper 23 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_24 :
    largeLower 24 ≤ largeLo (midpoint cut beta 24) ∧
      largeHi (midpoint cut beta 24) ≤ largeUpper 24 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_25 :
    largeLower 25 ≤ largeLo (midpoint cut beta 25) ∧
      largeHi (midpoint cut beta 25) ≤ largeUpper 25 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_26 :
    largeLower 26 ≤ largeLo (midpoint cut beta 26) ∧
      largeHi (midpoint cut beta 26) ≤ largeUpper 26 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_27 :
    largeLower 27 ≤ largeLo (midpoint cut beta 27) ∧
      largeHi (midpoint cut beta 27) ≤ largeUpper 27 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_28 :
    largeLower 28 ≤ largeLo (midpoint cut beta 28) ∧
      largeHi (midpoint cut beta 28) ≤ largeUpper 28 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_29 :
    largeLower 29 ≤ largeLo (midpoint cut beta 29) ∧
      largeHi (midpoint cut beta 29) ≤ largeUpper 29 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_30 :
    largeLower 30 ≤ largeLo (midpoint cut beta 30) ∧
      largeHi (midpoint cut beta 30) ≤ largeUpper 30 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_31 :
    largeLower 31 ≤ largeLo (midpoint cut beta 31) ∧
      largeHi (midpoint cut beta 31) ≤ largeUpper 31 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

end WuSource.SrcFourEnclosure
