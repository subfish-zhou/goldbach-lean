import SrcFourEnclosureData

noncomputable section
open Wu2008DoubleSieve FourRoughClosedMass
namespace WuSource.SrcFourEnclosure

theorem cell_large_32 :
    largeLower 32 ≤ largeLo (midpoint cut beta 32) ∧
      largeHi (midpoint cut beta 32) ≤ largeUpper 32 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_33 :
    largeLower 33 ≤ largeLo (midpoint cut beta 33) ∧
      largeHi (midpoint cut beta 33) ≤ largeUpper 33 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_34 :
    largeLower 34 ≤ largeLo (midpoint cut beta 34) ∧
      largeHi (midpoint cut beta 34) ≤ largeUpper 34 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_35 :
    largeLower 35 ≤ largeLo (midpoint cut beta 35) ∧
      largeHi (midpoint cut beta 35) ≤ largeUpper 35 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_36 :
    largeLower 36 ≤ largeLo (midpoint cut beta 36) ∧
      largeHi (midpoint cut beta 36) ≤ largeUpper 36 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_37 :
    largeLower 37 ≤ largeLo (midpoint cut beta 37) ∧
      largeHi (midpoint cut beta 37) ≤ largeUpper 37 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_38 :
    largeLower 38 ≤ largeLo (midpoint cut beta 38) ∧
      largeHi (midpoint cut beta 38) ≤ largeUpper 38 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_39 :
    largeLower 39 ≤ largeLo (midpoint cut beta 39) ∧
      largeHi (midpoint cut beta 39) ≤ largeUpper 39 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_40 :
    largeLower 40 ≤ largeLo (midpoint cut beta 40) ∧
      largeHi (midpoint cut beta 40) ≤ largeUpper 40 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_41 :
    largeLower 41 ≤ largeLo (midpoint cut beta 41) ∧
      largeHi (midpoint cut beta 41) ≤ largeUpper 41 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_42 :
    largeLower 42 ≤ largeLo (midpoint cut beta 42) ∧
      largeHi (midpoint cut beta 42) ≤ largeUpper 42 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_43 :
    largeLower 43 ≤ largeLo (midpoint cut beta 43) ∧
      largeHi (midpoint cut beta 43) ≤ largeUpper 43 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_44 :
    largeLower 44 ≤ largeLo (midpoint cut beta 44) ∧
      largeHi (midpoint cut beta 44) ≤ largeUpper 44 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_45 :
    largeLower 45 ≤ largeLo (midpoint cut beta 45) ∧
      largeHi (midpoint cut beta 45) ≤ largeUpper 45 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_46 :
    largeLower 46 ≤ largeLo (midpoint cut beta 46) ∧
      largeHi (midpoint cut beta 46) ≤ largeUpper 46 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

theorem cell_large_47 :
    largeLower 47 ≤ largeLo (midpoint cut beta 47) ∧
      largeHi (midpoint cut beta 47) ≤ largeUpper 47 := by
  norm_num [largeLower,largeUpper,largeLo,largeHi,
    largeQLo,largeQHi,smallWLo,smallWHi,logHi,logLo,
    midpoint,point,Finset.sum_range_succ,cut,alpha,beta,lam,
    truncatedSixthLowerAlpha,truncatedSixthLowerBeta,truncatedSixthLowerLambda]

end WuSource.SrcFourEnclosure
